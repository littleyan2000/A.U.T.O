import { ValidationError } from '../types/AppTypes';

export class ValidationManager {
    public validateAll(): ValidationError[] {
        // Placeholder, no validation for now
        return [];
    }
}